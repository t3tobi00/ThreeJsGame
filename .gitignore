# Claude Code internal files — workspaces, skills, scratch
.claude/

# Claude "superpowers" planning notes
docs/superpowers/

# OS noise
.DS_Store
**/.DS_Store

# All Markdown (internal design notes, brainstorms, dev logs).
# README at the repo root is the only one we share publicly — leading
# slash anchors the exception to root so subdirectory README.md files
# (design/.../README.md etc.) stay ignored.
*.md
!/README.md
