{
    "_doc": "Wood Storage — visible storage prop for the wood resource. PR #4.0 (revised 2026-05-07): now uses the same InventoryStack + StackSystem pipeline as the player so logs stack identically (uniform shape, spring physics, top-surface anchor). slotCapacity 30 caps the visible tower; 3 slots × 30 gives room for ~90 logs side-by-side once we want fuller capacity. anchorOffset.y = 0.55 sits the stack base just above the pad's top surface (pad mesh size.y = 0.4, mesh-centered). Visual ID: amber body + golden rim cap (storage-pad preset) so wood vs essence is readable even when the stack is empty.",
    "type": "WoodStorage",
    "mesh": {
        "preset": "storage-pad",
        "color": "0xc97a2a",
        "rimColor": "0xffb347",
        "size": { "x": 2.4, "y": 0.4, "z": 2.4 }
    },
    "iconResource": { "type": "wood", "height": 0.75, "scale": 1.0 },
    "components": {
        "Tag": { "tags": ["storage", "wood-storage", "structure", "static"] },
        "InventoryStack": {
            "maxSlots": 1,
            "slotCapacity": 30,
            "acceptsTypes": ["wood"],
            "anchorOffset": { "x": 0, "y": 0.55, "z": 0 },
            "slotSpacing": 0.45
        }
    }
}
