{
    "_doc": "Prototype-mode zombie. Extends legacy enemy.json with §4 numbers: HP 30, 5 DMG, 1 atk-sec, 8-tile aggro. Speed 3 (was 6 in legacy) — clearly slower than the player (speed 8) so the player can outrun a single zombie but is challenged by multiple. Drops are inherited (single 'meat' which the FEATURE_FLAGS.USE_ESSENCE_DROPS swap turns into essence). The spawn block is also inherited but PrototypeStateMachine overrides it per state. Mesh + WalkAnim swapped for the rigged 'character-zombie' preset so PlayerAnimSystem drives the lurch and LungeAnimSystem swings arms on each ContactDamage hit.",
    "extends": "enemy",
    "mesh": { "preset": "character-zombie", "color": "0x6a8a4a" },
    "components": {
        "Health":        { "hp": 45, "maxHp": 45, "armor": 0 },
        "Movement":      { "speed": 3, "controller": "simple_steering", "faction": "enemy" },
        "ContactDamage": { "damage": 2, "cooldown": 1.5, "range": 2.0, "targetFactions": ["player", "ally", "structure"] },
        "Spitter":       { "range": 5.0, "cooldown": 2.2, "damage": 5, "windupDuration": 0.45, "projectileSpeed": 9, "arcHeight": 1.2, "targetFactions": ["player", "ally", "structure"] },
        "EnemyAI":       { "aggroRadius": 8, "herdRadius": 5, "wanderSpeed": 0.6, "wanderRadius": 12, "wanderPauseMin": 1, "wanderPauseMax": 3 },
        "WalkAnim":      { "bobHeight": 0.05, "bobFreq": 4, "tiltAngle": 0.10, "style": "zombie" }
    }
}
