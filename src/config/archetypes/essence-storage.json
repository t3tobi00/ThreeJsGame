{
    "_doc": "Essence Storage — visible storage prop for the essence resource. Same pattern as wood-storage. Reuses the player's stacking pipeline (InventoryStack + StackSystem) so essence orbs stack identically to how the player would carry them.",
    "type": "EssenceStorage",
    "mesh": {
        "preset": "wall",
        "color": "0x224466",
        "size": { "x": 2.4, "y": 0.4, "z": 2.4 }
    },
    "components": {
        "Tag": { "tags": ["storage", "essence-storage", "structure", "static"] },
        "InventoryStack": {
            "maxSlots": 1,
            "slotCapacity": 30,
            "acceptsTypes": ["essence"],
            "anchorOffset": { "x": 0, "y": 0.55, "z": 0 },
            "slotSpacing": 0.45
        }
    }
}
